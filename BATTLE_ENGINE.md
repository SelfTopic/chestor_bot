# Боевой движок - разбор на атомы

Отдельный от `BATTLE_DESIGN.md` файл специально под фазу 4-5 (сам боевой
движок) - чтобы не листать полтысячи строк общего дизайн-дока в поисках
того, что конкретно осталось решить про бой. Общие решения (мультипликаторы
голода/кагуне, уклонение, смерть, левел-ап) здесь не повторяются целиком -
только ссылка + то, что из них следует конкретно для движка.

## Как читать

Каждый пункт помечен:
- ✅ **решено** - смотреть обоснование в `BATTLE_DESIGN.md`, здесь только вывод.
- 🔲 **открыто** - нужно решить, этим и занимаемся в этом файле.
- ❓ **обнаружено сейчас** - всплыло при структурировании, раньше нигде не
  фиксировалось (ни как решённое, ни как открытое) - то есть настоящий
  пробел, а не забытый пункт.

## Часть 0 - фундаментальная договорённость

Бой - **честная посимвольная симуляция раундов**, не одна формула на исход
(нужно для будущей анимации/косметики). Оба гуля действуют в КАЖДОМ раунде
одновременно; порядок отображения событий одного раунда - косметика, не
влияет на исход. Число раундов ограничено (`MAX_ROUNDS`) - бой не может
зависнуть, как в старой системе. См. `BATTLE_DESIGN.md` → "Модель боя:
раунды и лог событий".

---

## Часть 1 - Подготовка к бою (до раунда 1)

1.1. **Как вообще инициируется бой между двумя гулями?** ✅ решено - ровно
     два легальных способа PvP:
     - **Дуэль (по согласию)** - команда, например "дуэль", соперник
       указывается РЕПЛАЕМ (только там, где оба состоят в одном чате) ИЛИ
       упоминанием `@username` - второй способ работает откуда угодно, в
       том числе из ЛС инициатора с самим ботом (общий чат с оппонентом
       не нужен). Оппоненту приходит кнопка "согласен", бой начинается
       только после нажатия.
       ✅ **Таймаут на неответ/отказ - решено: 1 минута**, дальше
       автоматический отказ (тот же паттерн, что уже принят для исхода
       боя - "1 минута → отпустить").
     - ✅ **Предусловие: у бота должна быть открыта ЛС с ОБЕИМИ
       сторонами** - решено. Кнопка "согласен", выбор "всерьёз/фора"
       (1.5) и итог боя едут в ЛС; без гарантии доставки FSM ломается
       непредсказуемо где-то в середине, поэтому дешевле отказать сразу,
       чем ловить `TelegramForbiddenError` посреди флоу. Если инициатор
       ИЛИ приглашаемый ни разу не писали боту в личку - бой отменяется
       ДО отправки приглашения, инициатору отвечают, что сопернику нужно
       один раз написать боту в ЛС (`/start` подходит, но подходит и
       любое другое сообщение). Инфраструктура для проверки уже
       существует и не требует новой колонки/миграции: `User.
       has_private_chat` (`SyncEntitiesService.sync` уже проставляет
       `True` на любое сообщение от пользователя в приватном чате).
     - **Билеты (без согласия)** - будущая фича магазина: платный "билет",
       позволяющий атаковать любого БЕЗ его согласия, но только если цель
       "боеспособна" (не мертва). Цена сознательно высокая - чтобы сильным
       богатым игрокам не было выгодно систематически избивать слабых
       (та же анти-абьюз логика, что и у лимитов на переводы). Не реализуем
       сейчас - магазина ещё нет, но название механики и её ограничение
       стоит держать в голове при проектировании самого движка боя (движку
       всё равно, кто инициировал - согласием или билетом).

     Отдельно, как факт на будущее (НЕ проектируем сейчас): в далёком
     будущем планируется модуль "районов" с механикой "стенка на стенку" -
     все гули одного чата против всех гулей другого. Просто чтобы не забыть,
     что движок в теории должен когда-нибудь выдержать бой не 1-на-1.

     **Бои с мобами** - отдельный, не PvP, путь инициации:
     - Игрок сам ищет бой командой (например "бой с мобом") - это и есть уже
       решённый "фарм мобов" из `BATTLE_DESIGN.md` (статы моба случайны
       0.5x-2x от своих, раз в 10 минут), просто теперь с конкретным именем
       команды.
     - **Засада во время охоты за человеком** - моб-гуль может напасть и
       принудить к бою, пока игрок ест человека. Это и есть уже отмеченная
       в `BATTLE_DESIGN.md` фаза "3b" ("Поесть человека" с риском - в
       отличие от безопасной "3a"). Ждёт готового движка, как и было
       написано.

1.2. **Лимиты на бои** ✅ - 5 боёв/сутки на пару, 20/сутки всего, фарм мобов
     не считается. `BATTLE_DESIGN.md` → "Формула левел-апа".
     ✅ Таблица и подсчёт готовы (см. 5.1 - `BattleRepository.
     count_total_since`/`count_pair_since`). 🔲 Сама проверка лимита перед
     стартом боя ещё не вызывается ни из одного роутера - вместе с
     роутером дуэлей (внешний слой).

1.3. **Снапшот статов на старте** - какие статы идут в бой и через какую
     цепочку модификаторов:
     - `dexterity` (уклонение) ✅ формула есть, см. часть 2.
     - `strength` + `kagune_strength` (урон) 🔲 формула открыта, см. часть 2.
     - `health`/`max_health` - **уточнено, отменяет старую формулировку
       ниже**: `health` теперь ТОЖЕ идёт через полную цепочку модификаторов
       (база → голод → тип кагуне → какудж), СОВЕРШЕННО ТАК ЖЕ, как
       `strength`/`dexterity`/`speed`/`regeneration` - никакого отдельного
       механизма-исключения не нужно, `health` просто был последним статом,
       который в эту цепочку не был явно включён. ✅ РЕШЕНО автором явно
       (не отложенная фича, как было записано раньше ошибочно) - это
       **реальное, немедленное** увеличение боевого HP-пула, а не косметика:
       гуль с профильными 1000/1000 может честно НАЧАТЬ бой с эффективным
       пулом 1400 (при полном HP до боя), если тип кагуне это поддерживает
       (Ринкаку 1.5x, Бикаку 1.4x - см. `BATTLE_DESIGN.md`).
       **Важно**: `max_health` САМ (число-потолок, которое видно в профиле)
       по-прежнему НЕ умножается - это по-прежнему вакуумная/косметическая
       величина (см. 4.2, `mock_combat_power` суммирует его как есть).
       Умножается именно `health` (текущий боевой запас) - потому и
       получается "1400 при потолке профиля 1000": эффективный пул честно
       превышает вакуумный максимум, ровно как у любого другого стата.
       **Уточнено лором**:
       "выносливость" в `scripts/damage_calculate.py` - это не физическая
       усталость, а способность какухо мобилизовать RC-клетки; при
       физическом повреждении (потеря конечности/кагуне) гуль теряет
       способность мобилизации и уходит в гомеостаз, чтобы не умереть от
       кровопотери. Это 1-в-1 то, что уже делает `health`/`max_health`
       (0 HP = не может ходить/бить/поглощать урон, см. 2.6) - новый стат
       не нужен, это переименование смысла существующего под UI/копирайт
       (например в будущей команде "боевая мощь", 8.4, назвать поле
       "выносливость", а не голое "здоровье").
     - **База цепочки для health - `Ghoul.health` (ТЕКУЩЕЕ, не
       `max_health`) - ✅ решено (найдено как баг задним числом, см. чат).**
       Раньше `BattleService.ghoul_to_fighter` брал `max_health` - гуль,
       только что закончивший бой на 1 HP (3.1), начинал бы КАЖДЫЙ
       следующий бой при полном пуле, полностью игнорируя пассивную
       регенерацию здоровья между боями (`PASSIVE_STATS_CONFIG`,
       `materialize_passive_stats`) - весь смысл health как медленно
       восстанавливающегося ресурса терялся, а 3.1 становился декоративным
       (проигравший теряет HP в БД, но это ни на что не влияет). Теперь
       база - честное текущее `health` (уже досчитанное
       `materialize_passive_stats` до актуального момента внутри
       `GhoulService.get`), которое НИКОГДА не бывает больше `max_health`
       и никогда не роняется пассивной регенерацией ниже уже имеющегося
       значения - `validate_snapshot` не пробивается. Побочный эффект:
       ранг/мощь гуля В БОЮ (`BattleService.power_of`) теперь может
       честно отличаться от ранга в профиле (`GhoulService.calculate_power`,
       который по-прежнему от `max_health` - стабильный вакуумный
       показатель) - совпадают только при полном HP.
     - **`FighterSnapshot.max_health` - ✅ добавлено отдельным полем (найдено
       как баг на живом тесте, см. чат).** Побочный эффект решения выше:
       раз `FighterSnapshot.health` стал ТЕКУЩИМ (не вакуумным), любой код,
       которому нужна честная вакуумная "мощность" бойца независимо от
       текущего HP, больше не мог опираться на этот же снапшот -
       `MobService.generate_mob` (масштабирует моба от вакуумных статов
       игрока, см. 4.5/`MOB_CONFIG`) продолжал читать `.health` как раньше
       и молча унаследовал баг: игрок с искусственно раздутым (или
       просевшим после боя) текущим HP получал такого же неестественного
       моба. `FighterSnapshot` получил отдельное поле `max_health`
       (по умолчанию = `health`, для обратной совместимости везде, где
       разница не важна) - `ghoul_to_fighter` кладёт туда `ghoul.max_health`,
       `generate_mob` теперь масштабирует здоровье моба именно от него.
       Сам боевой расчёт (`compute_effective_stats`, цепочка модификаторов
       выше) по-прежнему работает от `health` - `max_health` в снапшоте
       чисто информационный, движком не используется.
     - Цепочка модификаторов база → голод-тир → тип кагуне → какудж уже
       решена и реализована для регена (`health_regen_per_hour`) - те же
       множители применяются и к боевым статам. ✅ переиспользовать как есть.
     - **Участвует ли `regeneration` В САМОМ бою** (тикает между раундами)?
       ❓ - никогда не обсуждалось. По умолчанию предлагаю считать бой
       "мгновенным" внутри игрового мира (одна непрерывная стычка) - реген
       не тикает между раундами, только между боями. Не решаю сам, это
       предположение для обсуждения.

1.4. **Роль `speed`** ✅ РЕШЕНО - теперь есть реальная боевая роль: шанс
     нанести ДОПОЛНИТЕЛЬНЫЙ удар за раунд (см. 2.4a). Никакого отдельного
     базового значения (в отличие от уклонения, 2.3) - шанс всегда чисто из
     соотношения скоростей двух бойцов:
     ```
     diff = max(speed_A, speed_B) - min(speed_A, speed_B)
     шанс(у кого speed меньше) = diff / max(speed_A, speed_B)
     шанс(у кого speed больше) = 2 × diff / max(speed_A, speed_B)
     ```
     При равных скоростях diff=0 → у обоих 0% (никому лишний удар не
     положен, это честно - нет разницы, нет преимущества).

     ✅ **РЕШЕНО (было 🔲 "нужен потолок 100%")** - вместо клэмпа проценты
     свыше 100 конвертируются в ЦЕПОЧКУ гарантированных + вероятностных
     ударов, а не обрезаются:
     ```
     pct = шанс(быстрого/медленного) в процентах  # может быть > 100
     guaranteed = 0
     while pct > 100:
         pct -= 100
         guaranteed += 1
     # guaranteed ударов бьются гарантированно, plus ещё один
     # с вероятностью pct% (остаток после вычитания сотен)
     ```
     Пример: 180% → 1 гарантированный удар + 80% шанс на третий (180-100=80).
     Каждый удар в цепочке - ЧЕСТНЫЙ, ровно как первый: та же независимая
     проверка уклонения (2.3), никакой разницы в обработке. При текущей
     формуле (`2×diff/max`, верхняя граница чуть ниже 200%) максимум
     получается 1 базовый + 1 гарантированный + 1 вероятностный = до 3
     ударов за раунд - но сам алгоритм цепочки общий, не завязан на "ровно
     3", и переживёт будущие правки формулы без изменений.

     Убирать стату по-прежнему НЕЛЬЗЯ - но теперь и не пришлось бы: у неё
     появилась осязаемая боевая роль, вопрос оправданности цены прокачки
     (был открыт в 8.2) теперь решается сам собой - `speed` прямо влияет на
     количество ударов в бою, обосновывать больше нечего.

1.5. **Выбор "всерьёз / дать фору" при большом перевесе - ✅ решено.**
     Прямое следствие механизма выравнивания кривой перевеса силы (6.2):
     сжатие HP-пула (`Battle._compress_hp_pools`) - единственная часть
     этого выравнивания, которую игрок реально **видит и держит в голове**
     ("у меня 21000 HP, я на 100%"). Остальное сжатие (урон/хил/уклонение/
     гейт/лишний удар) - абстрактные броски, игрок их не считает, их можно
     жать молча. А урезать HP-пул сильному игроку без его ведома - значит
     "драться со слабым не в полную силу без его согласия", чего делать
     нельзя.

     **Флоу дуэли (по согласию):**
     - Оба согласились → движок считает `power_ratio = сумма эфф. боевых
       статов сильного / сумма эфф. боевых статов слабого` (те же 5 боевых
       статов, что идут в бой, суммой; грубая оценка - игрок и сам
       прикинет это по своим числам).
     - `power_ratio >= 2.0` → сильному в ЛС приходит выбор + таймер 30 сек:
       - **Всерьёз** → `Battle(compress_hp=False)` - HP-пул не сжимается
         (сжатие остальных статов остаётся). По симуляции - ~100% победа
         сильного при 2x и выше, вопрос только в скорости.
       - **Дать фору** → `Battle(compress_hp=True)` - откалиброванная
         кривая 6.2 (2x → ~72%, у слабого реальный шанс).
       - Нет ответа за 30 сек → автоматически **фора** (сказано в самом
         сообщении).
     - `power_ratio < 2.0` → выбор не предлагается, всегда `compress_hp=
       True`. HP тут срезается меньше чем вдвое, "ограбления" не
       чувствуется.
     - Слабому игроку **не показывается ничего** - ни про перевес, ни про
       выбор силача. Списываем на опыт сильного гуля ("чует" слабого) +
       будущую настройку "скрыть статы" (8.4) + бой всё равно всё покажет.
     - Формулировка сообщения - **честная**, без "хотя кто знает": "Ты
       примерно в {ratio}x сильнее по боевым статам. Всерьёз - почти
       наверняка раздавишь. С форой - будет настоящий бой, шанс у него
       появится, но фаворит всё равно ты. Не выберешь за 30 сек - деремся
       с форой."

     **Бои с мобами:** всегда `compress_hp=True`, выбора нет. Лор: моб
     нападает из-под тишка, когда ты не готов бросить все силы в бой
     (та же "фаза 3b" из `BATTLE_DESIGN.md`). Статы моба и так ограничены
     0.5x-2x, экстрима почти не будет.

     **Готово в движке:** `Battle.__init__(compress_hp: bool = True)` -
     безопасный дефолт "с форой". `_compress_hp_pools` вызывается только
     при `compress_hp=True`; сжатие статов в формулах (`raw_damage`/
     `apply_heal`/dodge/gate/extra_hit) - всегда, независимо от флага.
     **Отложено (внешний слой, вместе с роутерами дуэлей):** расчёт
     `power_ratio`, ЛС с кнопками, таймер 30 сек (тот же FSM-паттерн, что
     "дуэль → кнопка согласен", 1.1).

---

## Часть 2 - Что происходит внутри одного раунда

2.1. Кто действует - ✅ оба одновременно каждый раунд. **Уточнено**: "действие"
     бойца за раунд - не обязательно один удар. У типов кагуне будет разная
     частота атак (см. 2.4a) - раунд честно симулирует ПАЧКУ под-ударов на
     каждого бойца, а не один обмен.

2.2. Порядок в логе раунда - ✅ косметика, по `speed`, ничья монеткой.
     **Уточнено**: если у бойца несколько под-ударов за раунд (2.4a), сами
     под-удары НЕ показываются игроку по отдельности - и лог, и UX видят
     только агрегированный итог раунда на бойца (например "Укаку → 5 атак,
     4 попадания, 40 урона из 70 возможных"). Симуляция внутри честная
     по-удочно, представление - агрегатное.

2.3. **Уклонение** ✅ - формула есть и зафиксирована:
     ```
     base(dex) = clamp(5 + (dex / 10000) * 90, 5, 95)
     weaker, stronger = min(dex_A, dex_B), max(dex_A, dex_B)
     dodge(послабее)  = base(weaker)
     dodge(посильнее) = clamp(base(weaker) * (stronger / weaker), 5, 95)
     ```
     Пол/потолок 5-95% всегда, guard на деление на 0. Известный побочный
     эффект (насыщение зависит от абсолютного `weaker`, не только от
     отношения) - принят, требует проверки симуляцией вместе с уроном.

     🔲 **Возможная будущая правка**: `speed` может позже войти в эту
     формулу вторым фактором (см. 1.4) - не меняем сейчас, но при добавлении
     формулу придётся пересобрать заново, не просто "домножить".

2.4. **Урон и защита** ✅ РЕШЕНО ПОЛНОСТЬЮ - был самым важным нерешённым
     пунктом всего движка, теперь закрыт целиком (2.4a-2.4d ниже). Разбор -
     из живого обсуждения на основе лора (`scripts/damage_calculate.py` +
     каноничное описание прочности тела гуля).

     Обнаружено при сверке лора в `scripts/damage_calculate.py` с текущей
     таблицей "Множители типов кагуне" (`BATTLE_DESIGN.md`): плоский
     "+1.8x к фиксированному списку статов, не стакается, без штрафов" не
     умеет выразить то, что лор описывает для каждого типа (частота атак,
     явные слабости, а у Коукаку - прямое противоречие: лор даёт урон
     [`strength`] + слабость по скорости, таблица - бонус к `dexterity`+
     `speed` и ничего к `strength`). Разобрано на два отдельных под-атома:

     2.4a. **Частота атак** ✅ РЕШЕНО ПОЛНОСТЬЮ (механизм И числа, задним
     числом отменяет прошлую версию этого пункта) - никакой отдельной
     таблицы "N ударов на тип" не нужно. Частота атак = чистая функция от
     `speed` через формулу лишнего удара (1.4). Тип кагуне влияет на неё
     ТОЛЬКО тем, насколько сильно он поднимает `speed` - у Укаку самый
     высокий множитель скорости (1.8x, см. таблицу в `BATTLE_DESIGN.md`),
     поэтому Укаку статистически чаще всех получает второй удар - именно
     это и означает "бьёт чаще" из лора, без единой лишней сущности в
     движке. Лишний удар - полноценный удар, идёт через тот же бросок
     уклонения (2.3), UX видит только агрегат (2.2).

     Приятное следствие сохраняется: слабость Укаку из лора ("однообразие,
     можно подстроиться и переждать") получается сама - у противника с
     хорошим уклонением каждый из потенциально двух ударов Укаку независимо
     может не попасть, тогда как редкий сильный удар Ринкаку либо весь
     проходит, либо весь мимо.

     2.4b. **Ослабления по типу** ✅ РЕШЕНО - явный штрафной множитель (<1.0)
     не понадобился. Вместо "бонус/штраф" разница между типами теперь
     чисто в ВЕЛИЧИНЕ бонуса к скорости: Укаку 1.8x, Ринкаку 1.5x, Бикаку
     1.4x, Коукаку 1.3x (самый низкий из всех, см. таблицу ниже) - раз
     шанс лишнего удара считается СРАВНЕНИЕМ скоростей двух бойцов (1.4),
     "самый слабый буст скорости из всех типов" уже и есть честная,
     эмерджентная слабость Коукаку по скорости из лора - отдельный минус
     не нужен, движку не пришлось учиться отнимать, только по-разному
     прибавлять.

     2.4c. **Формула одного удара (урон + защита)** ✅ РЕШЕНО - канон:
     обычным ножом/кулаком гуля почти не поранить (кожа держит), пробить
     может только кагуне-оружие или очень массивный тупой удар. Отсюда -
     физическая атака и кагуне-атака принципиально разного калибра, и
     защита от них тоже разная. Полная цепочка на КАЖДЫЙ (под-)удар,
     включая лишние из 2.4a:

     ```
     1. Уклонение (2.3, чистая dexterity) - если сработало, урона нет,
        дальше ничего не считается.
     2. Если удар долетел - гейт "успел ли защищающийся поднять кагуне":
        chance(защ) = min(100%, 50% × (dex+speed защ) / (dex+speed атак))
        (при dex+speed защ = 2× от атакующего - гарантированные 100%,
        при равных статах - 50%; не выбор игрока, честный бросок)
     3. Тип атаки - отдельная честная монетка 50/50: физическая сила или
        кагуне (тоже не выбор игрока - "и руками, и кагуне бьют вперемешку,
        кагуне не всегда быстрее рук")

     Дальше - 4 ветки по (поднял кагуне? × чем бьют?):

     - Кагуне поднят + бьют физически:
         block = random(45%, 75%)  # неизвестна вложенная сила удара,
                                    # берём случайность вместо неё
     - Кагуне поднят + бьют кагуне:
         margin = kagune_strength(защ) - kagune_strength(атак)
         block = random(10%, 20%) если margin >= 0, иначе 0%
         # 0% = "чужое кагуне рвёт моё" (лор), маленький блок при margin>=0 -
         # удар всё равно приходится по живой части тела
     - Кагуне НЕ поднят + бьют физически:
         margin = health(защ) - health(атак)
         block = random(10%, 20%) если margin >= 0, иначе 0%
         # то же самое рассуждение, что и для кагуне, только через health -
         # голое тело тоже может держать несильный удар
     - Кагуне НЕ поднят + бьют кагуне:
         block = 0%  # тело без кагуне-защиты кагуне-оружие не держит вообще

     raw = random(0.9, 1.1) × (
         strength(атак)                              если бьют физически
         strength(атак) + Σkagune_strength(атак)      если бьют кагуне
     )
     final_damage = raw × (1 - block/100)
     ```

     `margin >= 0` - это ПОРОГ (сравнение "своя сила ≥ чужой"), а не сама
     вероятность в процентах - в черновике использовалось слово
     "вероятность", но реально это булева развилка (проходит/не проходит
     порог), величина margin на размер блока (10-20%) не влияет.

     2.4d. **Валидация статов перед/во время боя** ✅ РЕШЕНО - на всякий
     случай (`dexterity`/`speed` в БД `default=1, nullable=False`, ноль
     штатно невозможен, но не гарантирован навечно): если у гуля в момент
     расчёта обнаружены невозможные статы (например ≤0 там, где идёт
     деление) - бой просто **отменяется** целиком, а не роняет деление на
     0 и не продолжается с мусорными числами.

2.5. **Криты** 🔲 открыто/не начато - в черновом наброске структуры события
     упоминается "тип действия: атака/уворот/крит", но сам факт "будут ли
     криты" и формула не решены. Можно сознательно отложить на v2, движок
     проживёт и без них.

2.6. **Что происходит, когда чьё-то HP доходит до 0 посреди боя** ✅ решено:
     это и есть естественный конец боя, вариант (b) - боец с HP=0 больше не
     способен ходить, наносить и впитывать урон, и считается проигравшим.
     Раунды дальше не идут - бой заканчивается сразу, не дожидаясь
     `MAX_ROUNDS`. Не противоречит "никого нельзя убить одним ударом",
     потому что к этому моменту оба уже честно получили равное число попыток
     ответить - решает не первый удар, а вся серия раундов до этой точки.

     🔲 Мелкие технические детали, не блокируют дизайн, но нужно не забыть
     при реализации:
     - HP не уходит в минус - клэмп на 0 (как и остальные статы в проекте).
     - **Одновременный обоюдный нокаут** - оба действуют в раунде
       одновременно, значит теоретически оба могут дойти до 0 в один и тот
       же раунд. Нужно явное правило (например: ничья невозможна в принципе,
       и в этом случае решает `MAX_ROUNDS`-принцип "у кого HP было выше до
       обмена" или просто монетка) - не решено, но не критично для старта
       проектирования формулы урона.

2.7. Как отличить "естественный" конец (кто-то дошёл до 0, ✅ решено выше) от
     "принудительного" (кончились раунды, решает остаток HP, ✅ описано в
     `BATTLE_DESIGN.md`) - оба случая теперь определены.

---

## Часть 3 - Итог боя

3.1. Проигравший заканчивает на 1 HP, не умирает - ✅.

3.2. Победитель выбирает: ограбить / отпустить / съесть - ✅. Ограбить =
     % от `User.balance`, переиспользует `change_balance_atomic` +
     `balances_log`. Съесть = убивает, переиспользует уже готовый
     `apply_death(cause="eaten", killer_telegram_id=...)`.

3.3. Таймаут выбора - 1 минута → авто-"отпустить" - ✅.

---

## Часть 4 - Награды

4.1. `level_progress` за победу - ✅ формула `1% × (сила_противника / своя_сила)`,
     см. `BATTLE_DESIGN.md` → "Формула левел-апа". Левел-ап максимум на 1 за
     раз, излишек выше 100% отбрасывается - ✅ уже так работает
     (`apply_level_progress`).

4.2. **Что такое "сила" в этой формуле?** ✅ РЕШЕНО - **вакуумная**
     (`calculate_power()`, косметическая, без модификаторов голода/типа
     кагуне/какуджи). Раньше здесь было открыто (см. историю - обсуждалось
     живьём в чате, но ни разу не было записано в `BATTLE_DESIGN.md`, нашли
     этот пробел только при сверке для этого файла).

     Итоговое обоснование - физиологическое, не просто "чтобы было честно":
     `rc_money` НЕ валюта в чистом виде, а **избыток RC-клеток в теле гуля**
     (которые можно преобразовать в новый тип кагуне или в какуджу - см.
     обновлённое описание колонки в `BATTLE_DESIGN.md`). Косметическая сила
     - это приближённая оценка КОНЦЕНТРАЦИИ RC-клеток в теле (точно можно
     узнать только анализом крови, у нас - приблизительно, через статы).
     Эффективная/боевая сила - это то, сколько из этих клеток реально
     удаётся мобилизовать на бой ПРЯМО СЕЙЧАС (зависит от голода/какуджи).

     Значит: **кто победил** решает эффективная сила (честный бой по
     раундам, см. часть 2). А вот **сколько РЕАЛЬНО RC-клеток в теле** у
     проигравшего - вопрос физиологии, не боя, и это вакуумная величина.
     Эксплойт "снайпить голодающего кита" (лёгкий бой, но большая награда)
     этим не открывается, а получает физиологическое объяснение: истощённый
     противник ДЕЙСТВИТЕЛЬНО легче побеждается (реальная сила просажена
     голодом - подтверждено статистикой в `scripts/power_statistics.py`,
     без какуджи голод может увести реальную силу даже НИЖЕ косметической),
     но тело его при этом не стало содержать меньше RC-клеток - это разные
     физические величины, и разное поведение для них не эксплойт, а точная
     модель.

     Подтверждено дополнительно статистикой: без какуджи, при одинаковой
     сытости, разница в количестве открытых типов кагуне даёт умеренный
     разброс силы (1.98x-2.24x в среднем) - то есть бойцы с разным числом
     открытых кагуне, но без какуджи, реально сопоставимы и могут пытаться
     побеждать друг друга. Доминирующие факторы - какудж (в среднем 3.11x
     против 1.13x без) и тир голода (смертельный голод без какуджи может
     увести реальную силу НИЖЕ косметической - 0.78x в среднем, минимум
     0.40x в выборке).

4.3. **Съедение гуля после боя** (`eat_ghouls`) - ✅ решено полностью.
     `RC = вакуумная сила проигравшего (см. 4.2) × случайный коэффициент
     0.3-0.6`. Лор: тело физически нельзя доесть целиком - что-то всегда
     остаётся несъеденным, отсюда диапазон меньше 1.0, а не "вся сила
     проигравшего".

4.4. Дневные лимиты - ✅ 5/пара, 20/всего.

4.5. Фарм мобов - ✅ та же формула ÷5, статы моба случайны 0.5x-2x от своих,
     малый шанс 1-2 RC. Не считается в лимит боёв с игроками.

---

## Часть 5 - Данные и инфраструктура

5.1. **Таблица под лимиты боёв и историю** ✅ реализовано (найдено как
     реальный эксплойт в чате - твинк-дуэль + бой с мобом одновременно,
     см. "Модель боя" выше). Две таблицы, не одна: `active_battles` -
     эфемерный лок "занят прямо сейчас" (PK на `telegram_id`, атомарный
     `INSERT ... ON CONFLICT DO NOTHING RETURNING` + откат частичного
     захвата - закрывает именно тот эксплойт), и `battles` - постоянная
     история (winner "a"/"b"/None, `is_forced`, `winner_choice`
     "rob"/"release"/"eat", раздельные `reward_level_progress`/`reward_rc`/
     `reward_balance` - колонки под 5.2/5.3/5.4 заведены заранее, сама
     механика выбора победителя ещё не написана). `BattleRecordService`
     оборачивает обе таблицы (claim/release/is_busy + record/count).
     `count_total_since`/`count_pair_since` готовы под 1.2/4.4 - вызов
     перед стартом боя ждёт роутера дуэлей.

5.2. **Счётчик побед/поражений для профиля** ✅ данных достаточно - таблица
     `battles` из 5.1 (`count(*) where winner="a" and participant_a=X`,
     аналогично для "b" и для поражений), отдельные счётчики-колонки на
     `Ghoul` не обязательны. Сам счётчик в `распрофиль` ещё не подключен.

5.3. Ограбить → `change_balance_atomic` + `balances_log` - ✅ уже есть,
     готово к переиспользованию.

5.4. Съесть → `apply_death` + `death_log` - ✅ уже готово, включая
     `killer_telegram_id` - специально оставлено под это в фиче смерти.

---

## Часть 6 - Проверка формул

6.1. Как только появятся формулы урона/попадания - прогнать симуляцию тысяч
     виртуальных боёв на разных соотношениях силы (1:1, 1:2, 1:5, 1:10) и
     посмотреть фактический % побед - ✅ сделано, см. 6.2 ("кривая перевеса
     силы" - результат прогона симуляции и того, что с ним сделали).

6.2. **"Кривая перевеса силы" - ✅ решено.** Первая версия движка (голые
     формулы 2.3/2.4a/2.4c/2.4e без какой-либо компенсации) при прогоне
     симуляции показала обрыв: 1:1 давал честные 50%, но уже 1:1.1-1.2
     почти гарантировал победу более сильной стороне (85-95%), а 1:2 - все
     99.9-100%. Причина - НЕСКОЛЬКО механик одновременно (уклонение, лишний
     удар от speed, гейт кагуне-защиты, сам урон, регенерация) стабильно
     благоприятствуют более сильной стороне, и каждое такое небольшое
     преимущество за раунд почти гарантированно решает исход за счёт
     закона больших чисел (бой идёт ~7-10 раундов, накопленная разница
     растёт как N, шум - только как √N).

     Решали методом "от обратного": сначала задали целевые точки кривой
     (2x перевес по статам -> ~75% побед сильной стороне, а не 99.9-100%;
     симметрично 0.5x -> ~25%), потом бисекцией по симуляции подобрали
     единственный параметр, при котором фактическая кривая ложится на эти
     точки - `BATTLE_CONFIG.stat_sensitivity_exponent` (сейчас 0.03).

     Механизм - `compress_stat_advantage(own, other)` в
     `services/battle_engine/core/formulas.py`: если `own <= other`, отдаёт
     `own` без изменений (слабая сторона не трогается вообще); если
     `own > other` - сжимает `own` К `other` как `other * (own/other) **
     stat_sensitivity_exponent`. Масштабно-инвариантно (не зависит от
     абсолютных величин статов, только от их отношения) и не трогает
     зеркальные бои (`own == other` -> сжатие не действует).

     Применяется в силе удара (`raw_damage`/`raw_fast_attack_damage` -
     атакующий сравнивается с ЭТИМ конкретным защищающимся), в силе хила
     (`Fighter.apply_heal` - против чужой регенерации) и в самом HP-пуле
     (`Battle._compress_hp_pools` - единственный боевой стат, который
     иначе не проходит ни через одну формулу сравнения вообще, а по
     симуляции оказался САМЫМ крутым каналом в одиночку). Уклонение/гейт
     кагуне-защиты/лишний удар от speed сжимаются похожим, но более
     простым способом прямо внутри своих формул (`_compress_ratio`) - они
     и так уже были честным сравнением двух сторон, без нужды в
     asymметричном "чьё это преимущество" якоре.

     **Про HP-пул отдельно (важно):** сжатие HP - это ступенчатая функция
     около нуля (проверено симуляцией: `exponent=0` → ровно ~50% на всех
     ratio; `exponent=0.00001` → уже ~62%). Отдельной, более мягкой
     экспоненты для HP не бывает - health не проходит через уклонение/гейт/
     блок, поэтому любое, даже исчезающе малое, стабильное преимущество в
     HP докручивается за 7-10 раундов до почти-достоверности. Есть только
     "жать HP" (→ ~66-72% при 2x) или "не жать вообще" (→ ~100%). Из-за
     этой бинарности сжатие HP сделано **опциональным и с явным согласием
     игрока** при перевесе 2x+ - см. 1.5.

---

## Часть 7 - Точка расширения под будущие "особые навыки"

7.1. Не проектируем сейчас, но раундовые формулы должны допускать либо
     дополнительный тип события в раунде, либо модификатор поверх
     шанса-попадания/урона - ✅ уже заложено как требование.

---

## Часть 8 - UX и видимость сложности

Отдельно от самой механики - сложность боя сейчас **под капотом**
нормальная (раундовая симуляция спрятана от игрока, это правильно), но
почти вся она **невидима** для игрока в UI. Обсудили отдельно, что с этим
делать - не блокирует формулу урона (2.4), но важно не потерять.

8.1. **Случайный тип кагуне при рождении** ✅ остаётся осознанно - лор:
     "до рождения гены не выбирают, это генетическая лотерея; не нравится -
     умри и полагайся на лотерею снова" (рифмуется с уже решённым
     возрождением через смерть - только там она новая честная попытка).
     🔲 Открыто на будущее: сами ШАНСЫ выпадения каждого типа нужно будет
     перевзвесить (сейчас `_first_kagune()` - равномерный случайный выбор
     из 4 типов) - Бикаку (трогает все 4 боевых стата) объективно сильнее
     Укаку (только скорость), значит должен выпадать реже. Не блокирует
     остальной дизайн, чисто балансный таск на потом.

8.2. **`speed` как "стата без роли"** - ✅ остаётся навсегда, см. 1.4.
     Убирать нельзя. Цена/оправданность - на усмотрение автора отдельно.

8.3. **Команда "голод" - ✅ реализовано** (`feature/battle-ux`): теперь
     показывает не только процент и тир, но и АКТИВНЫЕ модификаторы этого
     тира (падающие/растущие множители) и итоговые эффективные значения
     статов после их применения - собираются через `BattleService.
     ghoul_to_fighter` (чисто ради посчитанных `EffectiveStats`, без
     самого боя). Дополняет "боевую мощь" (8.4): "голод" - про ПРИЧИНУ
     (тир и его множители), "боевая мощь" - про ИТОГ (все статы разом).

8.4. **Новая команда "боевая мощь" - ✅ реализовано** (`feature/battle-ux`,
     `combat_power.py`): через `send_rich_message` (та же инфраструктура,
     что уже используется в `распрофиль`, с тем же fallback на plain-text)
     - ДВЕ таблицы (`InputRichBlockTable`, Bot API 10.1+) рядом: "фиктивная"
     (вакуумные/паспортные значения, те же числа, что суммирует
     `GhoulService.calculate_power`) и "боевая" (значение → влияние голода →
     влияние кагуне на каждый стат) - самый правый столбец второй таблицы
     всегда равен реальному боевому значению (`EffectiveStats`), это и есть
     финальная формулировка от автора вместо более ранней "просто two
     columns рядом". Здоровье - "фиктивная" колонка берёт `max_health`
     (паспортный потолок), а базовое значение в "боевой" - ТЕКУЩЕЕ `health`
     (то, с чем боец реально входит в бой) - именно контраст между ними и
     есть цель команды. Разбивка по шагам цепочки модификаторов вынесена в
     переиспользуемый `compute_stat_breakdown`/`StatBreakdown`
     (`battle_engine/core/fighter.py`) - `compute_effective_stats`
     отрефакторен, чтобы строиться поверх него (числа не меняются, только
     теперь видны промежуточные шаги, не только финал).

     Заодно реализована смежная справочная команда "кагуне"/`/kagune`
     (не отдельная фича из 8.1-8.4, но того же UX-видимости толка) -
     статичная таблица "какой тип кагуне на какой стат и с каким
     множителем влияет" (`KAGUNE_TYPE_MULTIPLIERS`), с пометкой ★ на
     приоритетном стате каждого типа (правило стаков, см. 1.4/`BATTLE_DESIGN.md`).
     Живёт в `race_profile_router.py` (не отдельный роутер - один хендлер
     без входных параметров, привязки к конкретному гулю нет).

---

## Итог: с чего реально начинать

**Блокеров для запуска движка больше нет.** 2.4 (урон + защита) был
последним и теперь решён целиком - уклонение → гейт кагуне-защиты → тип
атаки → 4 ветки блока → сам урон. Всё, что раскладывает один удар на
атомы, теперь есть.

Решено полностью и РЕАЛИЗОВАНО (включая сам внешний слой - роутеры, не
только движок): 1.1 (инициация боя - команда приглашения с двумя
раздельными кнопками согласия, реплай/`@username`, работает и из ЛС
инициатора с ботом; таймаут приглашения 1 минута; предусловие "ЛС
открыта у обеих сторон" через `User.has_private_chat`), 1.2/5.1/5.2
(дневные лимиты проверяются перед стартом дуэли; таблицы
`active_battles`/`battles`/`duel_sessions` + `BattleRecordService`
реализованы и протестированы против реального Postgres; счётчики побед/
поражений в `распрофиль`, отдельно по дуэлям и по мобам - не смешиваются),
1.4/2.4a/2.4b/2.4c/2.4d (роль `speed`, частота атак через цепочку ударов,
ослабления по типу, полная формула удара, валидация статов), 1.5 (выбор
"всерьёз / дать фору" при перевесе 2x+ - секретный ЛС-шаг сильной
стороне, таймер 30 сек → авто-фора), 2.6 (HP=0 посреди боя), 3.1-3.3
(проигравший на 1 HP, выбор победителя "ограбить/отпустить/съесть" с
таймером 1 минута → авто-"отпустить", исход и все итоговые числа дублируются
в ЛС обоим участникам), 4.2 (какая "сила" в награде), 4.3 (формула RC за
поедание - 0.3-0.6 × вакуумная сила), 4.5 (реализован и роутер боя с
мобом - кулдаун 10 мин, честная персистентность HP, level_progress ÷5,
RC-дроп, CheSton-награда - см. `ECONOMY.md` часть 4), стаки при разных
множителях (приоритет + `min()`-fallback), `health` в цепочке
модификаторов (включая `FighterSnapshot.max_health` - см. 1.3), 6.2
(выравнивание кривой перевеса силы через `stat_sensitivity_exponent`),
8.3/8.4 (UX-видимость - расширенный "голод" + новая команда "боевая
мощь", `feature/battle-ux`).

**Не блокирует релиз, но осталось на потом:** мелкие тай-брейки (2.6),
криты (2.5, сознательно отложены), 8.1 (перевзвесить шансы типов кагуне
при рождении - балансный таск, не UX), засада моба во время еды ("3b" - у
движка теперь есть всё нужное, но сам триггер
из `eat_human.py` не подключен), билеты без согласия (1.1, ждёт магазина),
районы "стенка на стенку" и рейд-боссы (структурно другие задачи, см.
`BATTLE_DESIGN.md`), пересчёт множителя цены `max_health` в `ECONOMY.md`
части 2 (автор проверил на реальных числах - разница на практике не
ощущается, чинить не обязательно).

❓ **Настоящий пробел, обнаружен только сейчас, не проверялся до этого:**
и `ActiveBattle`-лок, и фоновые таймауты дуэли (`asyncio.create_task` в
`duel/background.py`) живут только в памяти процесса бота - при рестарте
(деплой, падение) любая дуэль, зависшая на середине (ждёт согласия/выбора
"всерьёз-фора"/выбора победителя), НИКОГДА не разрешится сама: таймаут-
задача, которая должна была её снять, исчезла вместе с процессом, а
строка в `active_battles` остаётся - оба участника застревают с "занят
другим боем" навсегда, без единого пути восстановления (нет ни
автовосстановления при старте, ни админ-команды снять чужой лок вручную).
Не блокирует ПЕРВЫЙ релиз (рестарты редки, дуэль живёт 1-3 минуты - окно
маленькое), но перед реальным продакшеном стоит либо добавить очистку
устаревших `active_battles`/`duel_sessions` при старте бота (по аналогии
с тем, как `NotificationTicker` восстанавливает своё расписание), либо
хотя бы админ-команду `/release_battle <id>` на крайний случай.
