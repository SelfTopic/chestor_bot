# Battle-модуль: дизайн и план

Живой документ. Обновляется по мере решений — не переспрашивать заново то, что
уже здесь зафиксировано, а дописывать/править.

Battle уже дважды начинали и бросали в этом проекте (см. `feature/battle` -
архивная ветка со старым мёртвым кодом, никогда не мерджилась). Оба раза
начинали с боевого движка - самой сложной и самой завязанной на всё
остальное части. В этот раз порядок обратный: сначала независимые от боя
механики, бой - в конце.

## Существующие колонки `Ghoul` и их назначение

| Колонка | Роль |
|---|---|
| `rc_money` | RC-клетки. Валюта для: прокачки уровня кагуне (вместе с обычными деньгами в "растить кагуне"), левел-апа гуля, открытия новых типов кагуне (всего 4, открываются по мере развития). Источник: поедание гуля после победы в бою (см. `eat_ghouls`). |
| `level` | Потолок прокачки статов. Чем выше уровень - тем выше доступный предел `upgrade_stat`. Цель: нельзя разбогатеть и мгновенно накачать статы до максимума - апгрейд ограничен уровнем, а левел-ап должен быть настоящим испытанием (не "подрался с твинком пару раз за день и поднял несколько уровней"). |
| `kagune_type_bit` | Битовая маска типов кагуне, которыми владеет гуль (можно несколько одновременно). Расшифровка - `calculate_kagune` в `src/bot/utils/kagune_calculate.py`. |
| `kagune_strength` | СЕЙЧАС: одно число на все типы кагуне сразу - недостаточно. НУЖНО: сила отдельно по каждому владеемому типу. Решение архитектуры - см. "Открытые вопросы". |
| `strength` | Физическая мускулатура. Влияет на наносимый урон. Не путать с `kagune_strength`. |
| `dexterity` | Ловкость. Влияет на % уклонения и на % успешного побега из боя (вместе с `speed`). |
| `speed` | Скорость. Влияет на очерёдность атаки (инициативу) и на % успешного побега (вместе с `dexterity`). |
| `health` | Текущее здоровье. Динамическая величина - меняется в бою, регенерирует со временем (см. `regeneration`). |
| `max_health` | Потолок `health`. |
| `regeneration` | Скорость восстановления `health` за период. Раньше было реализовано как отдельный бесконечный скрипт, который лечит всех не полных по здоровью гулей раз в тик - решили НЕ повторять этот подход (см. "Открытые вопросы" и фазу 2). |
| `eat_humans` | Счётчик съеденных людей. Пополняет `hunger`. |
| `eat_ghouls` | Счётчик съеденных гулей. Доступно только после победы над гулем в бою. Даёт больше `hunger`, чем человек, плюс начисляет RC-клетки и exp. |
| `is_kakuja` | Флаг превращения в какуджу - разовая "покупка" за большое количество RC-клеток, когда их накоплено достаточно. Даёт огромный прирост статов, но кратно увеличивает расход `hunger` (чаще приходится есть). |
| `hunger` | Голод. Пассивно уменьшается (ориентир: 4 раза в сутки по 4% = полностью голодным через неделю реального времени). Пополняется едой. Сильно влияет на статы: чем ниже голод, тем ниже `dexterity`/`regeneration`/`speed`, но выше `strength`/`kagune_strength` (риск/выгода, "звериное" состояние). |

## Новое, чего пока нет

- **`exp`** - новая колонка, опыт с боёв (с гулями и мобами). Источник левел-апа.
  Точная формула/пороги левел-апа пока не определены - открытый вопрос.
- **Хранение силы по типам кагуне** - см. открытые вопросы.
- **"Поесть человека"** - команда/механика, восстанавливает `hunger`, не
  требует существования боя.
- **Сам боевой движок** - инициатива по `speed`, урон по `strength` +
  `kagune_strength` с модификаторами от текущего `hunger`, уклонение/побег по
  `dexterity`+`speed`.

## Открытые вопросы (нужно решить до соответствующей фазы, не изобретать самому)

1. **Хранение силы кагуне по типам.** Варианты:
   - Отдельная таблица `ghoul_kagune_strength(ghoul_id, kagune_type, strength)`
     - расширяемо, требует новой миграции+репозитория.
   - 4 отдельные колонки в `Ghoul` (`ukaku_strength`, `koukaku_strength`, ...)
     - проще, но менее гибко на будущее.
   - **Статус: не решено.**
2. **Формула левел-апа / нужный exp по уровням.** Автор сам пока не знает,
   как именно это должно работать. **Статус: не решено, вернуться к фазе 5.**
3. **Механизм regen/hunger** - предложен ленивый расчёт (хранить снапшот +
   timestamp, считать актуальное значение по формуле в момент обращения,
   без фонового процесса) вместо старого подхода с бесконечным циклом.
   **Статус: предложено, ждёт подтверждения.**

## План по фазам

Каждая фаза выбрана так, чтобы её можно было построить и протестировать БЕЗ
готового боевого движка (кроме самой фазы 4, разумеется).

1. **Экономика и потолок.** `exp` колонка+миграция. RC-стоимость в
   `upgrade_kagune` (наравне с деньгами). Потолок `upgrade_stat` привязан к
   `level`. Тестируется без боя - RC можно выдавать вручную через `/set_stat`.
2. **Пассивные системы.** Голод и реген через ленивый расчёт (см. открытый
   вопрос 3), без отдельного постоянно работающего процесса.
3. **Поедание людей.** Восстанавливает голод, самостоятельная команда, не
   требует боя.
4. **Боевой движок.** Инициатива/урон/уклонение/побег по формулам выше. Можно
   свериться со старым кодом в `feature/battle` как с референсом (не факт,
   что напрямую переиспользуемо).
5. **Награды за бой.** Поедание гуля после победы → RC + exp → отсюда
   получает смысл левел-ап (см. открытый вопрос 2).
6. **Какуджа.** Разовый toggle за RC + статовые модификаторы + повышенный
   расход голода. Зависит от фазы 1 (RC-экономика) и фазы 2 (голод).
