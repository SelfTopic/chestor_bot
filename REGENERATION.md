# Регенерация в бою - отдельная комната

Вынесено из `BATTLE_ENGINE.md` (2.4e) - атомов в одном файле снова стало
слишком много, чтобы держать всё разом в голове. Здесь - **только**
регенерация и её ближайшие связи внутри уже готового движка
(`feature/battle-engine`, `src/bot/services/battle_engine/core/` -
переехал из `utils/battle_calculate.py` после рефакторинга архитектуры,
см. "Часть 3" ниже). Обо всём остальном (урон, блок, тип атаки,
кагуне-множители) - в `BATTLE_ENGINE.md`, сюда не тащим.

**⚠️ Часть 1 ниже описывает старое поведение (регенерация как "довесок в
конце раунда")** - механика с тех пор ПЕРЕОСМЫСЛЕНА (регенерация теперь
замещает основное действие раунда, плюс появился отдельный тип
`FastAttack`) - см. "Часть 3 - обновление после рефакторинга" в конце
файла за точным текущим поведением. Часть 1 оставлена как есть
(историческая точность), не переписана задним числом.

## Как читать

- ✅ **решено/реализовано** - работает как есть, код смёржен на
  `feature/battle-engine`.
- 🔲 **открыто** - надо решить.
- ❓ **обнаружено сейчас** - реальный пробел, не просто забытый пункт.

---

## Как было обнаружено

`compute_effective_stats` честно прогонял `regeneration` через всю цепочку
модификаторов (база → голод-тир → тип кагуне → какудж), но эту переменную
нигде дальше не читали - ни в уклонении, ни в уроне, ни в блоке. Кагуне-типы,
усиляющие регенерацию (Ринкаку 1.8x, Бикаку 1.4x - см. `BATTLE_DESIGN.md`
"Множители типов кагуне"), реально не давали в бою НИЧЕГО. Автор заметил
это сам, не я - см. историю чата.

---

## Часть 1 - Сама механика (✅ реализовано)

1.1. **Критический порог** ✅ - 10% от СТАРТОВОГО HP этого конкретного боя
     (`EffectiveStats.health`, посчитанный один раз в начале боя), а НЕ от
     вакуумного `max_health` из профиля (см. `BATTLE_ENGINE.md` 4.2 - та же
     логика разделения вакуум/эффективное, что и везде в движке).
     `BATTLE_CONFIG.critical_health_percent = 10.0`.

1.2. **Первый прок - гарантированный** ✅ - как только HP бойца (после
     урона за раунд, `hp > 0`) впервые оказывается ниже порога, лечение
     срабатывает без всякого броска. Лечит `0.9-1.1x` от эффективной
     регенерации (`BATTLE_CONFIG.regen_heal_variance_min/max` - та же форма
     случайности, что у `raw_damage`).

1.3. **Второй прок - вероятностный, по формуле лишнего удара `speed`** ✅ -
     если критический порог пересекается ВТОРОЙ раз за бой, лечение уже не
     гарантировано - шанс считается той же формулой, что и `extra_hit_percent`
     (1.4 в `BATTLE_ENGINE.md`): сравнение своей регенерации с регенерацией
     противника, у кого выше - шанс вдвое больше. Отличие от `speed`-версии -
     здесь **нет цепочки** на случай >100%, просто клэмп до 100% (см.
     `regen_proc_chance` - однократный бросок, не `resolve_hit_chain`).

1.4. **Ограничение "только 1 раз" на саму ПРОВЕРКУ** ✅ - решающая деталь,
     без которой вся механика ломается: вероятностный бросок (1.3) может
     случиться только ОДИН раз за весь бой, независимо от исхода (успех
     или провал). Если боец пересекает порог в третий, четвёртый и т.д.
     раз - лечения больше не будет никогда. Без этого ограничения боец с
     сильной регенерацией мог бы пересекать порог раз за разом (например
     оставаясь на 9% HP, каждый раунд немного проседая и почти долечиваясь)
     и не проигрывать вообще, пока не кончатся `MAX_ROUNDS`.
     **Итог: максимум 2 прока лечения на бойца за весь бой** (1 гарантия +
     максимум 1 удачный бросок).

1.5. **Не лечит уже проигравшего** ✅ - если `hp <= 0`, регенерация вообще
     не проверяется - естественный конец боя (2.6 в `BATTLE_ENGINE.md`)
     сильнее, реанимации нет.

1.6. **Не лечит выше стартового пула** ✅ - `min(starting_hp, hp + heal)`,
     никакого "оверхила" выше эффективного HP на начало боя.

Код (на момент первой реализации, до переезда - см. Часть 3):
`regen_proc_chance`, `RegenState`, `apply_regeneration` в
`battle_calculate.py`; состояние (`RegenState` на бойца) переживает раунды
внутри `simulate_battle`, как и `streak_a`/`streak_b` для типа атаки.
`RoundResult.regen_to_a`/`regen_to_b` - для логов/отладки. 8 тестов,
коммит `fdd4b5f`.

---

## Часть 2 - Ближайшие связи (пока не проговорено, есть что обсудить)

2.1. ❓ **Взаимодействие с фармом мобов.** Формула боя с мобом переиспользует
     тот же движок (см. `BATTLE_ENGINE.md` 1.1/4.5) - значит регенерация
     мобу тоже включена автоматически, раз у него те же статы (0.5-2x от
     игрока). Не обсуждалось явно, стоит просто подтвердить, что это
     нормально (мобы тоже "выживают" критический момент), а не баг.

2.2. 🔲 **Будущий навык, усиляющий регенерацию в бою?** По аналогии с уже
     упомянутым будущим навыком "мастерство кагуне" (ускоряет decay типа
     атаки, см. `BATTLE_ENGINE.md` 2.4b/`physical_attack_decay_percent`) -
     возможный кандидат: навык, снимающий ограничение "только 2 прока" или
     поднимающий сам процент критического порога (лечиться раньше, не
     дожидаясь 10%). Не решено, не спроектировано - просто напрашивающееся
     место для будущего расширения (см. `BATTLE_ENGINE.md` часть 7 -
     точка расширения под особые навыки).

2.3. 🔲 **10% - взято с потолка, не проверено симуляцией.** Как и
     `max_rounds`, `physical_attack_decay_percent` и другие числа в
     `BATTLE_CONFIG` - решена структура, не цифра. Стоит прогнать
     `scripts/battle_log_all_kagune.py`/`battle_simulation.py` с прицелом
     именно на Ринкаку/Бикаку (у них выше регенерация) - сработала бы
     регенерация вообще хоть раз в 15 боях с текущими числами?

2.4. ❓ **Не показано в UX вообще.** Регенерация проявляется только в логе
     отладочного скрипта - в реальном будущем бою (когда появится Telegram
     UX, см. `BATTLE_ENGINE.md` часть 8) игрок должен как-то увидеть "тебя
     спасла регенерация", иначе это невидимая механика, ровно та же
     категория проблемы, что уже обсуждалась про `speed`/голод (часть 8
     `BATTLE_ENGINE.md`).

---

## Часть 3 - обновление после рефакторинга архитектуры (актуальное поведение)

Движок переехал из `utils/battle_calculate.py` в
`services/battle_engine/core/` (классы вместо кучи функций в одном файле
- отдельное обсуждение, не про регенерацию саму по себе). Заодно
переосмыслена причинность: **регенерация теперь замещает основное
действие бойца за раунд** (Attack или Regen, решает
`Fighter.decide_action()` ПО HP НА НАЧАЛО раунда), а не довесок постфактум
после урона, как было в части 1 выше.

**Уточнение, которого не было в первой версии**: лишний удар от `speed`
(экс-`resolve_hit_count`, теперь `resolve_extra_hit_counts`) - это
ОТДЕЛЬНАЯ, всегда проверяемая надбавка, не связанная с тем, что боец
выбрал основным действием. Бонусные удары от скорости случаются даже в
раунде, где основное действие - `RegenAction`, и классифицируются как
`FastAttackAction` (не `AttackAction`) - специально, чтобы рендерер видел
разницу между "решил драться" и "просто оказался достаточно быстрым,
чтобы урвать удар походя". Резолвятся они тем же `resolve_hit`
(уклонение/гейт/тип атаки/блок не отличаются от обычной атаки), НО с
пометкой `is_fast_attack=True` - урон честно НИЖЕ (см. отдельное
обсуждение позже в этом же файле, часть 4 - "мелкие однообразные атаки"
из лора Укаку означали, что FastAttack не должен бить в полную силу).

Код теперь: `core/fighter.py` (`Fighter.decide_action`/`Fighter.apply_heal`,
`regen_guaranteed_used`/`regen_roll_used` как атрибуты объекта, не
отдельный `RegenState`), `core/formulas.py` (`regen_proc_chance` без
изменений), `core/actions.py` (`RegenAction`, `FastAttackAction`),
`core/battle.py` (`Battle._resolve_participant`). Пункты 1.1-1.6 выше по
сути не изменились (порог, гарантия, один вероятностный бросок, запрет
реанимации, клэмп по стартовому HP) - изменилось только КОГДА решение
принимается (до раунда, не после) и то, что бонусный удар теперь возможен
одновременно с `RegenAction`.

---

## Часть 4 - FastAttack не бьёт в полную силу (не про регенерацию саму по
себе, но напрямую задело этот файл)

Автор заметил в `scripts/damage_calculate.py`: лор Укаку прямо говорит
"атакует чаще МЕЛКИМИ однообразными атаками", а `FastAttackAction` до
этого наносил ровно столько же урона, сколько обычная `AttackAction` -
чистый вклад в `speed` позволял сносить 3-4 полных удара за раунд 1.

✅ РЕШЕНО - `FastAttackAction` теперь считает урон по отдельной, более
низкой И смещённой вниз случайности (`fast_attack_damage_variance`
0.5-0.8, вместо обычных `damage_variance` 0.9-1.1) - см.
`formulas.raw_fast_attack_damage`, вызывается из `resolve_hit(...,
is_fast_attack=True)`. Уклонение/гейт кагуне-защиты/тип атаки/блок не
меняются - разница исключительно в итоговом уроне. Проверено вживую:
одинаковый `strength`, обычная атака ~91, FastAttack ~53 в том же бою.

---

## Итог

Сама механика (часть 1, с уточнением из части 3) полностью реализована и
покрыта тестами - это НЕ блокер. Часть 2 - открытые связи, ни одна не
блокирует ничего прямо сейчас, можно разбирать по одной, когда дойдут
руки.
